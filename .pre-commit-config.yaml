repos:
  - repo: https://github.com/pre-commit/mirrors-ruff
    rev: v0.0.0  # Replace with the latest ruff version
    hooks:
      - id: ruff
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v0.0.0  # Replace with the latest mypy version
    hooks:
      - id: mypy