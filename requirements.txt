# Core ML/Audio Processing
torch>=2.0.0
transformers>=4.35.0
librosa>=0.10.0

# Audio manipulation
pydub>=0.25.1
# Note: pydub.playback may require simpleaudio or pygame for audio playback
# Install with: pip install simpleaudio (or pygame)

# Data processing
pandas>=2.0.0

# Text similarity
rapidfuzz>=3.0.0

# HTTP requests (for download.py)
requests>=2.31.0

# Faster Whisper (CTranslate2-based, 4x faster than standard transformers)
faster-whisper>=1.0.0

# Audio file I/O (required for faster-whisper segment processing)
soundfile>=0.12.0
